language: csharp
mono: none
dotnet: 2.0.0
dist: trusty
before_install:
  - cd .travis/
  - chmod +x build.sh
  - chmod +x test.sh
script:
  - cd .travis/
  - sudo ./build.sh --quiet verify
  - sudo ./test.sh --quiet verify
